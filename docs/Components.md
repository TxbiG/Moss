# Components
## Overview

## 3D
## CPUParticle3D
```c
```
Example
```cpp
```
| Variant | Methods |
| --- | --- |
| `git status` | List all *new or modified* files |
| `git diff` | Show file differences that **haven't been** staged |

## GPUParticle3D
```c
```
Example
```cpp
```
| Variant | Methods |
| --- | --- |
| `git status` | List all *new or modified* files |
| `git diff` | Show file differences that **haven't been** staged |

## Terrain
```c
```
Example
```cpp
```
| Variant | Methods |
| --- | --- |
| `git status` | List all *new or modified* files |
| `git diff` | Show file differences that **haven't been** staged |


## 2D

## CPUParticle2D
```cpp
CPUParticle2D CPUParticle2D_new();
```
Example
```cpp
```
| Variant | Methods |
| --- | --- |
| `git status` | List all *new or modified* files |
| `git diff` | Show file differences that **haven't been** staged |

## GPUParticle2D
```cpp
GPUParticle2D GPUParticle2D_new();
```
Example
```cpp
```
| Variant | Methods |
| --- | --- |
| `git status` | List all *new or modified* files |
| `git diff` | Show file differences that **haven't been** staged |


## Sprite2D
```cpp
Sprite2D sprite();
```
Example
```cpp
```
| Variant | Methods |
| --- | --- |
| `git status` | List all *new or modified* files |
| `git diff` | Show file differences that **haven't been** staged |

## Tilemap
```cpp
Tilemap tile();
```
Example
```cpp
```
| Variant | Methods |
| --- | --- |
| `git status` | List all *new or modified* files |
| `git diff` | Show file differences that **haven't been** staged |
